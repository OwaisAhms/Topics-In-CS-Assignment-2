# .env.example
# Ollama service URL:
OLLAMA_URL=http://localhost:11434
OLLAMA_CHAT_MODEL=llama3.1:8b
OLLAMA_EMBED_MODEL=nomic-embed-text

# Index paths (inside container these map to /data/ because of docker-compose)
INDEX_PATH=index.faiss
META_PATH=meta.json

# App guards
MAX_INPUT_LEN=2000